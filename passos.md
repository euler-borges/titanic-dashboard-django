# Passos da ultima etapa

### Primeiro: Modificar `models.py` com o Titanic passanger

### Rodar `python manage.py makemigrations` para gerar o arquivo que fará a migração do banco

### Rodar `python manage.py migrate` para efetivamente modificar o banco

### Criar superusuário `python manage.py createsuperuser`

### Inserir os dados do csv no json com `python script.py`

### "Restaurar backup"/popular banco de dados com o json com `python manage.py loaddata data.json`
